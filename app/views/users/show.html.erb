<% @books.each do |book| %>
  <tr>
    <td>
      <%= link_to user_path(@user) do %>
      <%= attachment_image_tag @user, :plofile_image, :fill, 50, 50, fallback: "no_image.jpg" %>
      <% end %>
    </td>
  </tr>
<% end %>